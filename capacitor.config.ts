import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'matima.game',
  appName: 'matima',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
